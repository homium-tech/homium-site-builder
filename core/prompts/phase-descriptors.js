/**
 * PhaseDescriptors — Facade de compatibilidad hacia el módulo profundo Pipeline (core/pipeline/index.js)
 */
const {
  STEP_ACTIONS,
  PhaseDescriptors
} = require('../pipeline');

module.exports = {
  STEP_ACTIONS,
  PhaseDescriptors
};
